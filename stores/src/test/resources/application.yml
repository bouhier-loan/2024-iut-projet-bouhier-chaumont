spring:
  application.name: stores
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:stores
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true